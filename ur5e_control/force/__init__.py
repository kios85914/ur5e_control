"""Force-control subsystem (Robotiq FT 300, forward-looking).

Pending hardware validation — interfaces and control law are implemented and
mock-tested; on-robot tuning happens when the FT 300 is mounted.
"""
